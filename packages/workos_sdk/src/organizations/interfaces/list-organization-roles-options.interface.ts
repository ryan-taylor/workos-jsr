/**
 * Options for retrieving roles associated with a WorkOS Organization.
 */
export interface ListOrganizationRolesOptions {
  /** The unique identifier of the organization to retrieve roles for */
  organizationId: string;
}
